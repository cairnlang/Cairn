# examples/concurrency/linked_failure.ax
# Intentionally fails: linked child exits and takes the linked caller down with it.

TYPE msg = Fail

SPAWN_LINK msg {
  SELF Fail SEND
  RECEIVE
    Fail { "child_failed" SWAP DROP EXIT }
  END
}
DROP
