# examples/practical/all_practical.ax — run the full practical pipeline chain
# Usage: mix axiom.run examples/practical/all_practical.ax

IMPORT "main.ax"
IMPORT "ledger.ax"
IMPORT "todo.ax"
IMPORT "expenses.ax"
IMPORT "cashflow.ax"
IMPORT "cashflow_alerts.ax"

# The imported practical chain currently leaves one intermediate value.
# Drop it so the consolidated runner remains stack-clean for tests.
DROP
