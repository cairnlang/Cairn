# examples/practical/lib/stats.crn — small reusable stats helpers

DEF score_total : [int] -> int EFFECT pure
  { ABS } MAP SUM
  POST DUP 0 GTE
END

DEF mean_int : [int] -> int EFFECT pure
  DUP SUM SWAP LEN DIV
END
